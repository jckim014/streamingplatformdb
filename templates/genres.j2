<html>

<head>
    <title>Genres</title>
    <link href="style.css" rel="stylesheet" type="text/css" />
</head>

<body>
    <h1>Genres</h1>
    <nav>
        <a href="/ ">Home</a> |
        <a href="shows">Shows</a> |
        <a href="platforms">Platforms</a> |
        <a href="showsonplatforms">ShowsOnPlatforms</a> |
        <a href="users">Users</a> |
        <a href="reviews">Reviews</a> |
        <a href="contenttypes">ContentTypes</a> |
        <a href="genres">Genres</a> |
    </nav>
    <p>&nbsp;</p>
    <div style="display:block">
        <p><strong>Browse Genres</strong></p>
        <table border="1" cellpadding="5">
        <thead>
            <tr>
                {% for key in data[0].keys() %}
                <th>{{ key }}</th>
                {% endfor %}
            </tr>
            <tbody>
            {% for item in data %}
            <tr>
                {% for key in item.keys() %}
                <td>{{item[key]}}</td>
                {% endfor %}
                <td><a href="edit_genre/{{item.genreID}}" class="btn btn-default">Edit</a></td>
                <td><a href="delete_genre/{{item.genreID}}" class="btn btn-default">Delete</a></td>
            </tr>
            {% endfor %}
        </table>
        <p>&nbsp;</p>
    </div>
    <div style="display: block">
        <form id="addGenre" action="/genres" method="post">
            <legend><strong>Add Genre</strong></legend>
            <fieldset class="fields">
                <label> description </label> <input type="text" name="description" required>
            </fieldset>
            <input class="btn" type="submit" value="Add a Genre" name="Add_Genre">
        </form>
    </div>
</body>

</html>